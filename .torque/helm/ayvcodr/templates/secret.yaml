apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ayvcodr.secretName" . }}
  labels:
    {{- include "ayvcodr.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}